package model;

public class ReadOnly extends UIProperty {

	public ReadOnly(String label, Integer length, Integer precision, ComponentKind componentKind, Boolean display) {
		super(label, length, precision, componentKind, display);
	}

}
