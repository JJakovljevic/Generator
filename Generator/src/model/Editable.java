package model;

public class Editable extends UIProperty {

	public Editable(String label, Integer length, Integer precision, ComponentKind componentKind, Boolean display) {
		super(label, length, precision, componentKind, display);
	}

}
